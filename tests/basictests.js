var assert = require('assert');
var thingFactory = require('../lossyfortunes');

suite('The basic stuff', function basicSuite() {
  test('Test of thing', 
    function testThing(testDone) {
      testDone();
    }
  );
});
